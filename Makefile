# Minimal make file to ensure install process is happy.
#
# Note that after an install the BLM-DI-gui key must be updated by running
#
#   configure-ioc e BLM-DI-gui /path/to/install/runopi.dls

install:
clean:
